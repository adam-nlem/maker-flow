import type { ComponentType } from "react";

export const clientTopBarActions: Record<string, ComponentType> = {
    // Per-route action components for the client topbar — add entries keyed by pathname (e.g. clientHomePath).
};
